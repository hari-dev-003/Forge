# Frontend config. Vite only exposes vars prefixed with VITE_. Copy to `.env`.
# Point this at your deployed API Gateway URL (no local backend).
VITE_API_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com/prod/api
